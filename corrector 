Para añadir la carpeta scrips:
grv
si no existe la rama ypstream
git remote add upstream https://github.com/Stolz/laravel
git fetch upstream
git merge upstream/develop siendo develop en la que estamos
por ultimo solucionamos los conflictos
gmty

./scripts/check_code -s

./vendor/bin/phpcbf app --standard=phpcodesniffer.xml --tab-width=4 --sniffs=Generic.WhiteSpace.ScopeIndent.Incorrect
cambiando Generic.WhiteSpace.ScopeIndent.Incorrect por el error que sea
gdt para ver los cambios